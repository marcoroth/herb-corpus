<%
  page_data(
    title: "Record failed outcome for #{teacher_full_name(@teacher)}",
    error: @record_fail.errors.any?,
    backlink_href: confirm_ab_teacher_record_failed_outcome_path(@teacher)
  )
%>

<%= render Teachers::RecordOutcomeComponent.new(
  mode: :appropriate_body,
  service: @record_fail
) %>
