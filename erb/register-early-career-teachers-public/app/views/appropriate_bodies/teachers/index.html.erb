<% page_data(title: @appropriate_body.name, error: false) %>

<%= render TeachersIndexComponent.new(
      appropriate_body_period: @appropriate_body,
      teachers: @teachers,
      pagy: @pagy,
      status: @status,
      query: params[:q]
    ) %>
