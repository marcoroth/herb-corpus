<% page_data(
  title: "Record passed outcome for #{teacher_full_name(@teacher)}",
  error: @record_pass.errors.any?,
  backlink_href: admin_teacher_induction_path(@teacher)
) %>

<%= render Teachers::RecordOutcomeComponent.new(
  mode: :admin,
  service: @record_pass
) %>
