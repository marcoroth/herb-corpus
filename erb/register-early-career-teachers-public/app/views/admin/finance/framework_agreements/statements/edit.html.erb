<%
  page_data(
    title: "Edit statement",
    error: @statement.errors.present?,
    backlink_href: admin_contract_period_framework_agreement_statement_path(@framework_agreement.contract_period, @framework_agreement, @statement)
  )
%>

<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_with model: @statement,
                   url: admin_contract_period_framework_agreement_statement_path(@framework_agreement.contract_period, @framework_agreement, @statement),
                   method: :patch do |f| %>
      <%= render "fields", form: f, framework_agreement: @framework_agreement, contract_period: @framework_agreement.contract_period %>
      <%= f.govuk_submit "Save" %>
    <% end %>
  </div>
</div>
