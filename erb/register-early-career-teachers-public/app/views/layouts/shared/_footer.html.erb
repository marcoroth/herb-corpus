<%=
  govuk_footer(
    meta_items: {
      'Accessibility' => '/accessibility',
      'Cookies' => '/cookies',
      'Privacy' => '/privacy',
      'Contact support' => current_user.try(:school_user?) ? new_schools_support_query_path : '/support'
    }
  ) do |footer|
    footer.with_meta_html do
      render DebugSessionComponent.new
    end
  end
%>
