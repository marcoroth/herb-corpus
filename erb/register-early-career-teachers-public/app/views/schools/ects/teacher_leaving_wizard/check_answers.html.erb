<% page_data(
    title: @wizard.current_step.heading_title,
    backlink_href: @wizard.previous_step_path
) %>

<p class="govuk-body"><%= @wizard.current_step.leaving_statement %></p>

<p class="govuk-body"><%= @wizard.current_step.edit_warning_statement %></p>

<%= govuk_button_to(
      "Confirm and continue",
      @wizard.current_step_path
    ) %>

<p class="govuk-body">
  <%= govuk_link_to(
        "Cancel and go back to #{@wizard.teacher_full_name}’s details",
        schools_ect_path(@wizard.ect_at_school_period),
        no_visited_state: true
      ) %>
</p>
