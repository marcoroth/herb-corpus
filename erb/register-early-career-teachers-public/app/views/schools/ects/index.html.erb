<%
  page_data(
    title: "Early career teachers (ECT)",
    error: false,
    caption: @school.name,
    caption_size: 'l'
  )
%>

<% if @school.blocked_from_registering_new_ects? %>
  <%= govuk_inset_text do %>
    <p class="govuk-body"><%= @school.name %> no longer has funding for early career teacher entitlement (ECTE). You cannot register new early career teachers.</p>
    <p class="govuk-body">Find out more about <%= govuk_link_to("eligibility and funding for ECTE", "https://www.gov.uk/guidance/funding-and-eligibility-for-ecf-based-training") %>.</p>
  <% end %>
<% else %>
  <%= govuk_button_link_to("Register an ECT starting at your school", schools_register_ect_wizard_start_path) %>
<% end %>

<hr class="govuk-section-break--m">

<div class="govuk-grid-row">
  <% if @number_of_teachers.zero? %>
    <div class="govuk-grid-column-two-thirds">
      <p class="govuk-body">Your school currently has no registered early career teachers.</p>
    </div>
  <% else %>
    <div class="govuk-grid-column-full">
      <%= render Shared::TopSearchComponent.new %>
      <% if @teachers.count.zero? %>
        <p class="govuk-body">There are no ECTs that match your search.</p>
      <% else %>
        <% @teachers.each do |teacher| %>
          <% ect_at_school_period = teacher.current_or_next_ect_at_school_period %>
          <%= render Schools::ECTs::ListingCardComponent.new(
            teacher: teacher,
            ect_at_school_period:,
            training_period: ect_at_school_period.current_or_next_or_latest_training_period,
            current_school: @school
          ) %>
        <% end %>
      <% end %>
      <%= govuk_pagination(pagy: @pagy) %>
    </div>
  <% end %>
</div>
