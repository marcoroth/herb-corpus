<% page_data(
    title: "When did or when will #{@ect.full_name} start teaching as an ECT at your school?",
    error: @wizard.current_step.errors.present?,
    backlink_href: @wizard.previous_step_path,
) %>

<%= form_with(model: @wizard.current_step, url: @wizard.current_step_path) do |f| %>
  <%= content_for(:error_summary) { f.govuk_error_summary } %>

  <%= f.govuk_date_field :start_date,
                         width: "two-thirds",
                         legend: { text: 'Start date', size: "m", tag: "h3", hidden: true },
                         hint: { text: "For example, 17 9 #{Date.current.year}" }
  %>

  <%= f.govuk_submit "Continue" %>
<% end %>
