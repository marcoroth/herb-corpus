<%=
  form.govuk_date_field :finished_on,
    legend: { text: finished_on_legend },
    hint: { text: "For example, 20 4 #{1.year.ago.year}" }
%>

<%= form.govuk_number_field(:number_of_terms,
  width: 4,
  suffix_text: 'FTE terms',
  label: { size: 'm', text: number_of_terms_label },
  hint: { text: "Enter induction terms between 0 and 16. You can use up to one decimal place if the term is not a whole number. For example, for 2 and a half terms enter 2.5" }
) %>

<p class="govuk-body">You'll need to consider:</p>

<%=
  govuk_list([
    "the amount of induction time they've completed so far",
    "their full-time equivalent (FTE) working patterns",
    "if they have sufficient time in their current post which can count towards their induction",
  ], type: :bullet)
%>
