<div class="govuk-summary-card">
  <div class="govuk-summary-card__title-wrapper">
    <h3 class="govuk-summary-card__title govuk-!-font-size-24">
      <strong><%= link_to_mentor %></strong>
    </h3>
  </div>
  <div class="govuk-summary-card__content">
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-one-half">
        <%= govuk_summary_list(borders: false, rows: [trn_row, assigned_ects_row]) %>
      </div>
      <% if show_training_details? %>
        <div class="govuk-grid-column-one-half">
          <% case training_status %>
          <% when :active %>
            <p class="govuk-body"><%= teacher_name %> is registered for ECTE mentor training with:</p>
            <%= govuk_summary_list(borders: false, rows: training_period_summary_rows) %>
          <% when :deferred %>
            <p class="govuk-body"><%= lead_provider_name %> have told us that <%= teacher_name %>'s ECTE mentor training is paused. Contact them if you think this is an error.</p>
          <% when :withdrawn %>
            <p class="govuk-body"><%= lead_provider_name %> have told us that <%= teacher_name %> is not registered for ECTE mentor training with them. Contact them if you think this is an error.</p>
            <p class="govuk-body">Tell us if <%= teacher_name %> <%= link_to_mentor("will be training with another lead provider") %>.</p>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>
