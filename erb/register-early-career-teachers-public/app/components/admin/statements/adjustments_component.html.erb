<div class="finance-panel govuk-!-margin-top-5 govuk-!-margin-bottom-5">
  <%= govuk_table do |table| 
    table.with_caption(text: "Additional adjustments", size: "m") 

    table.with_head do |head|
      head.with_row do |row|
        row.with_cell(header: true) { "Payment type" }
        row.with_cell(header: true, classes: %w[govuk-!-display-none-print]) { "" }
        row.with_cell(header: true, numeric: true) { "Payments" }
      end
    end

    table.with_body do |body|
      adjustments.each do |adjustment|
        body.with_row do |row|
          row.with_cell { adjustment.payment_type }
          row.with_cell(numeric: true, classes: %w[govuk-!-display-none-print]) { change_or_remove_link(adjustment) }
          row.with_cell(numeric: true) { number_to_pounds(adjustment.amount) }
        end
      end
    end
  end %>

  <% if adjustment_editable? %>
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-one-half govuk-!-display-none-print">
        <%= add_adjustment_link %>
      </div>
        
      <div class="govuk-grid-column-one-half app-adjustments-total-column">
        <%= total %>
      </div>
    </div>
  <% else %>
    <%= total %>
  <% end %>
</div>
