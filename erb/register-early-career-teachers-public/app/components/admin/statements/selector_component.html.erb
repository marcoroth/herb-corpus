<%= form_with url: choose_admin_finance_statements_path do |f| %>
  <div class="app-admin-filter">
    <%= f.govuk_collection_select :lead_provider_id,
      lead_providers,
      :id,
      :name,
      label: { text: "View a different provider" },
      options: { selected: lead_provider_id, include_blank: false },
      form_group: { class: 'filter-form-group' }
    %>

    <%= f.govuk_collection_select :contract_period_year,
      contract_periods,
      :id,
      :year,
      label: { text: "View a different contract year" },
      options: { selected: contract_period_year, include_blank: false },
      form_group: { class: 'filter-form-group' }
    %>

    <%= f.govuk_collection_select :statement_date,
      statement_dates,
      :id,
      :name,
      label: { text: "View a different statement" },
      options: { selected: statement_date, include_blank: false },
      form_group: { class: 'filter-form-group' }
    %>

    <%= f.govuk_submit "View", secondary: true, class: "filter-button" %>
  </div>
<% end %>
