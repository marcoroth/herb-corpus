Konflikte: <%# Block.conflicted_appointments.to_a.count %><br>
Blöcke: <%# blocks.to_a.count %><br>

<br>

<% if false %>
  <% blocks.group_by {|b| b.start_time.to_s(:time) }.sort.each do |blocks| %>
    <%= blocks.first %> - <%= blocks.last.count %> -  <%= blocks.last.map { |b| b.start_time.wday }.uniq.sort %><br>
  <% end %>
<% end %>

<br><br>
