<div class="col-12 col-xl-8">
  <h3 class="text-muted">Mitglieder</h3>

  <div class="row">
    <% @group.users.each do |user| %>
      <%= render UserStatusComponent.new(user: user, extra_classes: "col-12") %>
    <% end %>
  </div>
</div>
