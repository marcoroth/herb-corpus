<div class="col-12 col-xl-8">
  <h3 class="text-muted">Konflikte</h3>

  <%= render CardComponent.new(classes: "tw-p-6") do |card| %>
    <% card.with_card_content { "Keine Konflikte" } %>
  <% end %>
</div>
