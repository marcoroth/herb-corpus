<% content_for :page_title, @group.name %>
<% content_for :page_subtitle, Group.translated_name %>

<div class="main-content tw-p-2">
  <%= render "header" %>

  <div class="container-fluid">
    <div class="row">
      <%= render partial: "groups/pages/appointments" %>
      <%= render "info" %>
    </div>
  </div>
</div>
