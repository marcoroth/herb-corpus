<div class="col-12">
  <h3 class="text-muted">Kalender</h3>

  <%= render CardComponent.new do |card| %>
    <%= card.with_body do %>
      <div class="fullcalendar" data-ics-url="<%= ical_user_path(@user.ical_key, no_update: "true") %>"></div>
    <% end %>
  <% end %>
</div>
