<div class="col-12 col-xl-8">
  <h3 class="text-muted">Aufträge</h3>

  <%= render CardComponent.new(classes: "tw-p-6") do |card| %>
    <% card.with_card_content { "Keine Aufträge" } %>
  <% end %>
</div>
