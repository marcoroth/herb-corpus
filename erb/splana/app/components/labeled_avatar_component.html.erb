<div class="col-auto">
  <%= render AvatarComponent.new(user: @user, size: @size) %>
</div>

<div class="col-auto ml-n2">
  <h4 class="card-title mb-1"><%= @title %></h4>
  <p class="card-text small text-muted"><%= @description %></p>
</div>
