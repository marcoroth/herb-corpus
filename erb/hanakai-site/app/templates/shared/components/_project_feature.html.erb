<% logo_rotation = rand(-15..15) %>

<section class="flex flex-col lg:flex-row lg:items-stretch gap-6 lg:gap-0">
  <%# Bubble (top on mobile, left/right on desktop) %>
  <div class="flex-1 lg:flex lg:flex-col lg:justify-center lg:z-10 lg:relative <%= 'lg:order-last' if reversed %>">
    <p class="text-xl font-bold text-primary px-8 mb-4"><%= title %></p>

    <div class="pf-bubble relative flex flex-col lg:block">
      <div
        class="
          bg-neutral border-3 b-primary rounded-2xl p-8 shadow-lg relative
          <%= reversed ? 'lg:ml-[-0.75rem]' : 'lg:mr-[-0.75rem]' %>
        "
      >
        <%= render_slot :description %>

        <div
          class="
            absolute bottom-0 translate-y-1/2 w-5 h-5 rotate-45
            bg-neutral border-b-3 border-r-3 b-primary rounded-br-md
            <%= reversed ? 'right-18' : 'left-18' %>
          "
        ></div>
      </div>

      <div
        class="hidden lg:block lg:absolute lg:top-full mt-5 <%= reversed ? 'lg:right-10' : 'lg:left-10' %>"
        style="rotate: <%= logo_rotation %>deg"
      >
        <% if logo == "dry" %>
          <%= render "svgs/dry_logo_animated", width: 80, height: 80, class_name: "block", trigger_selector: ".pf-bubble" %>
        <% elsif logo == "rom" %>
          <%= render "svgs/rom_logo_animated", width: 72, height: 80, class_name: "block", trigger_selector: ".pf-bubble" %>
        <% else %>
          <%= render "svgs/hanami_logo_animated", width: 80, height: 76, class_name: "block", trigger_selector: ".pf-bubble" %>
        <% end %>
      </div>
    </div>
  </div>

  <%# Code (bottom on mobile, right/left on desktop) %>
  <div class="flex-1 min-w-0 flex items-center <%= 'lg:order-first' if reversed %>">
    <div
      class="
        flex-1 -mt-10 lg:mt-10 bg-secondary-subtle border border-[oklch(from_var(--hk-color-bg-secondary)_l_c_h/0.2)] rounded-lg
        shadow-xs overflow-hidden
      "
    >
      <div class="content content--panel <%= reversed ? 'content--panel--left' : 'content--panel--right' %>">
        <%= raw Site::Content::Markdown.render(code) %> <%# herb:disable erb-no-unsafe-raw %>
      </div>
    </div>
  </div>
</section>
