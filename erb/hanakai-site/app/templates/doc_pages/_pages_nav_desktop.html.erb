<div
  class="
    hidden lg:block
    lg:overflow-y-auto flex-1 pr-4 scrolling-panel
    lg:scrolling-panel--with-bottom-overlay
  "
  data-defo-ensure-active-nav-link-visible="<%= { id: "desktop-pages-nav" }.to_json %>"
>
  <div class="sticky top-0">
    <div
      class="
        hk-project-heading bg-neutral flex gap-3 pb-2 items-center mt-1
        pl-[1px] pt-[1px]
      "
    >
      <%= render "svgs/#{org}_logo_animated", width: 32, height: 32, trigger_selector: ".hk-project-heading" %>

      <p class="text-primary text-2xl leading-tight -ml-[1px]">
        <%= render "shared/names/#{org}" %>
      </p>
    </div>

    <div
      class="
        bg-linear-to-b from-(--hk-color-bg-neutral) to-transparent h-4
        pointer-events-none
      "
    ></div>
  </div>

  <nav class="hidden lg:block"><%= render_slot :nav %></nav>
</div>
