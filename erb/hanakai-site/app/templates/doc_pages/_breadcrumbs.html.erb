<nav
  aria-label="Breadcrumb"
  class="
    font-mono text-xs font-semibold text-primary tracking-wider uppercase
    pt-3 lg:pt-0.5 mb-3 flex justify-between
  "
>
  <ol class="flex gap-2 items-center">
    <% breadcrumbs.each do |breadcrumb| %>
      <% list_classes = "hidden" if breadcrumb.root %>

      <li class="<%= list_classes %>">
        <% if breadcrumb.url %>
          <a class="hover:underline" href="<%= breadcrumb.url %>">
            <%= breadcrumb.label %>
          </a>
        <% else %>
          <%= breadcrumb.label %>
        <% end %>
      </li>

      <% if breadcrumb != breadcrumbs.last %>
        <li class="<%= list_classes %>">
          <%= render "svgs/icons/arrow_right", width: 16, height: 16, class_name: "fill-current -mt-0.5" %>
        </li>
      <% end %>
    <% end %>
  </ol>

  <div class="flex gap-2 order-2 sticky top-3 self-start items-center">
    <% if version %>
      <%= render "doc_pages/version_select",
                            id: "version-select-mobile",
                            testid: nil,
                            version: version,
                            version_links: version_links,
                            anchor: "right" %>
    <% end %>

    <%= render "svgs/#{org}_logo", width: 28, height: 28, class_name: "lg:hidden" %>
  </div>
</nav>
