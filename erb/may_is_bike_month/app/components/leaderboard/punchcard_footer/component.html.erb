<div
  class="
    flex justify-between mt-7 text-purple-300 dark:text-purple-400 font-mono
    text-[13px] tracking-[0.12em]
  "
>
  <div class="flex items-center gap-2">
    <span class="uppercase">less</span>

    <span class="inline-grid grid-cols-6 gap-1">
      <% swatches.each do |swatch| %>
        <%= render(UI::Tooltip::Component.new(text: swatch[:title_imperial])) do |tooltip| %>
          <% tooltip.with_body do %>
            <span class="unit-imperial"><%= swatch[:title_imperial] %></span>
            <span class="unit-metric hidden"><%= swatch[:title_metric] %></span>
          <% end %>

          <i class="punchcard-cell block h-5 w-5 cursor-help" <% if swatch[:level] %> data-l="<%= swatch[:level] %>" <% end %>></i>
        <% end %>
      <% end %>
    </span>

    <span class="uppercase">more</span>
  </div>

  <% if @updated_at %>
    <div class="text-right">
      updated
      <%= render(UI::Time::Component.new(time: @updated_at, format: :localize_time_precise)) %>
    </div>
  <% end %>
</div>

<% if @competitions.many? %>
  <%= render(Leaderboard::OtherCompetitionLinks::Component.new(competitions: @competitions, current_competition: @competition)) %>
<% end %>
