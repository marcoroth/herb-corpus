<%= form_with(url: "#", builder: MayIsBikeMonthFormBuilder) do |f| %>
  <%= render(Form::Group::Component.new(form_builder: f, attribute: :name, kind: :text_area, html_options: {rows: 4})) %>
<% end %>
