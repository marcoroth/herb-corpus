<div
  class="relative inline-block"
  data-controller="ui--dropdown"
  data-ui--dropdown-placement-value="<%= floating_ui_placement %>"
>
  <button
    type="button"
    class="<%= button_classes %>"
    id="<%= button_id %>"
    aria-expanded="false"
    data-action="click->ui--dropdown#toggle"
    data-ui--dropdown-target="button"
  >
    <span class="sr-only">Open <%= @name %> menu</span>
    <%= button_content %>

    <span class="dropdown-chevron inline-block transition-transform duration-200">
      &#9662;
    </span>
  </button>

  <div
    class="
      hidden bg-white dark:bg-purple-800 rounded-lg shadow-lg border
      border-purple-200 dark:border-purple-700 min-w-44 z-50
    "
    data-ui--dropdown-target="menu"
  >
    <ul role="menu" class="dropdown-menu" aria-labelledby="<%= button_id %>">
      <% entries.each do |entry| %>
        <%= entry %>
      <% end %>
    </ul>
  </div>
</div>
