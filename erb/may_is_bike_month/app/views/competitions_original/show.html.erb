<%= render partial: "competitions_original/#{@year}" %>

<%= render(Leaderboard::OtherCompetitionLinks::Component.new(competitions: @competitions, current_competition: @competitions.find { |c| c.year == @year.to_i }, original_view: true)) %>
