<div class="ribbon" id="ribbon">
  <div class="ribbon-container <%= Rails.env %>">
    <span class="env"><%= Rails.env.capitalize %></span>

    <span class="version">
      Rails: <strong><%= Rails.version %></strong>, Ruby:
      <strong><%= RUBY_VERSION %></strong>
    </span>
  </div>
</div>
