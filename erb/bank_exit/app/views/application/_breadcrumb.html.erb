<% if defined?(breadcrumbs) && breadcrumbs.count > 1 %>
  <%# locals: (size: nil) %>
  <nav
    aria-label="Breadcrumb"
    class="breadcrumbs w-full print:hidden! <%= size %>"
    id="breadcrumb"
  >
    <ul>
      <%= render_breadcrumbs separator: '', tag: :li %>
    </ul>
  </nav>
<% end %>
