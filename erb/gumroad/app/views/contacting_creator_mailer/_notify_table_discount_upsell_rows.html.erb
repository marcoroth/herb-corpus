<% display_offer_code = @display_offer_code || @offer_code %>
<% if @offer_code&.code.present? %>
  <tr>
    <td>
      <h4>Discount</h4>
    </td>
    <td><%= @offer_code.name %> <span style="white-space: nowrap;">(<%= display_offer_code.displayed_amount_off(@product.price_currency_type, with_symbol: true) %> off)</span></td>
  </tr>
<% end %>
<% if @offer_code&.upsell.present? %>
  <tr>
    <td>
      <h4>Upsell</h4>
    </td>
    <td><%= @offer_code.upsell.name %> <span style="white-space: nowrap;">(<%= display_offer_code.displayed_amount_off(@product.price_currency_type, with_symbol: true) %> off)</span></td>
  </tr>
<% elsif @purchase.upsell_purchase.present? %>
  <tr>
    <td>
      <h4>Upsell</h4>
    </td>
    <td><%= @purchase.upsell_purchase.upsell.name %></td>
  </tr>
<% end %>
